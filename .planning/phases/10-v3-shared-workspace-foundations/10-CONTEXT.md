---
phase: 10-v3-shared-workspace-foundations
type: context
status: ready
---

## Goal

Build the first shared-workspace foundations for `v3` so multiple agents and
humans can share memory without flattening scope, trust, or provenance.

## Current Boundary

- `v2` is now complete enough to hand off into federated memory work.
- `memd` remains the memory control plane, not the cognition stack.
- shared memory must preserve private/public boundaries and auditability.

## Dependencies

- branch strategy and OSS workflow are already in place
- `v1` repair/provenance is complete
- `v2` reversible compression and Obsidian compiled evidence workspace are complete

