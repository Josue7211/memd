# Plan 24-01: `v4` Hook Context Current-Task Defaults

## Goal

Bring the hook-context path onto the same short-term default as the rest of the
bundle.

## Steps

1. Default `memd hook context` to `current_task` when intent is omitted.
2. Update Unix and PowerShell hook scripts to use `current_task` by default.
3. Update hook README guidance.
4. Record the phase completion in `.planning`.

## Verification

- `cargo test -q -p memd-client`
- `cargo test -q`

## Result

The standard hook path now participates in the short-term memory hot path by
default.
