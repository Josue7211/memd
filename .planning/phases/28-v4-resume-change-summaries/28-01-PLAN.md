# Plan 28-01: `v4` Resume Change Summaries

## Goal

Persist and surface short-term resume deltas.

## Steps

1. Persist the latest hot-lane snapshot under `state/last-resume.json`.
2. Compare the current snapshot to the previous one during resume.
3. Surface a compact delta in prompt and bundle memory markdown.
4. Keep the output focused on the hot lane.
5. Verify resume rendering behavior.

## Verification

- `cargo test -q -p memd-client`
- `cargo test -q`

## Result

Operators can tell what changed since the last pickup without replaying
history.
