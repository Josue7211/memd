---
phase: 01-v1-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/memd-server/src/main.rs
  - crates/memd-server/src/store.rs
  - crates/memd-server/src/inspection.rs
  - crates/memd-server/src/repair.rs
  - crates/memd-server/src/working.rs
  - crates/memd-schema/src/lib.rs
  - crates/memd-client/src/main.rs
  - crates/memd-client/src/render.rs
  - crates/memd-client/src/commands.rs
  - docs/api.md
  - docs/architecture.md
  - docs/source-policy.md
  - docs/promotion-policy.md
autonomous: true
requirements:
  - QUAL-04
  - QUAL-05
  - WORK-02
  - WORK-03
  - INTG-04
user_setup: []
must_haves:
  truths:
    - "Operators can drill from an explain response to the underlying source artifact metadata without leaving memd."
    - "Operators can run explicit repair actions for stale, contested, superseded, and malformed memory state."
    - "Working memory reports admission, eviction, and rehydration decisions with deterministic policy reasons."
  artifacts:
    - path: "crates/memd-server/src/inspection.rs"
      provides: "Provenance and explain helpers extracted from the server entrypoint"
      min_lines: 120
    - path: "crates/memd-server/src/repair.rs"
      provides: "Repair and lifecycle helpers for verify, expire, supersede, contest, and metadata correction"
      min_lines: 120
    - path: "crates/memd-server/src/working.rs"
      provides: "Working-memory policy helpers and trace selection"
      min_lines: 120
    - path: "crates/memd-client/src/render.rs"
      provides: "Shared CLI render helpers for working memory, profile, source, and explain output"
      min_lines: 80
    - path: "crates/memd-client/src/commands.rs"
      provides: "Command routing helpers split out of the CLI entrypoint"
      min_lines: 80
  key_links:
    - from: "crates/memd-server/src/main.rs"
      to: "crates/memd-server/src/inspection.rs"
      via: "server route handlers delegate explain/provenance work to a focused module"
      pattern: "mod inspection|inspection::"
    - from: "crates/memd-server/src/main.rs"
      to: "crates/memd-server/src/repair.rs"
      via: "server route handlers delegate repair lifecycle work to a focused module"
      pattern: "mod repair|repair::"
    - from: "crates/memd-client/src/main.rs"
      to: "crates/memd-client/src/render.rs"
      via: "CLI summary rendering uses extracted formatting helpers"
      pattern: "render_working_summary|render_profile_summary|render_source_summary"
---

<objective>
Finish the remaining `v1` memory gaps while reducing the size of the two entrypoints that are making the repo hard to work in.

Purpose: Close provenance, repair, and working-memory gaps honestly before claiming `v2`, while breaking the large server/client files into reusable seams where the architecture already supports it.
Output: Focused server helper modules, slimmer client render/command helpers, stable explain/repair/workflow contracts, and docs that match the implemented behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-v1-completion/01-CONTEXT.md
@.planning/phases/01-v1-completion/01-DISCUSSION-LOG.md

@crates/memd-schema/src/lib.rs
@crates/memd-server/src/main.rs
@crates/memd-server/src/store.rs
@crates/memd-client/src/main.rs
@docs/api.md
@docs/architecture.md
@docs/source-policy.md
@docs/promotion-policy.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extract provenance and repair server helpers</name>
  <files>crates/memd-server/src/main.rs, crates/memd-server/src/inspection.rs, crates/memd-server/src/repair.rs, crates/memd-schema/src/lib.rs, docs/api.md</files>
  <read_first>crates/memd-server/src/main.rs, crates/memd-server/src/store.rs, crates/memd-schema/src/lib.rs, docs/api.md, docs/architecture.md</read_first>
  <action>Move the explain/provenance path and the repair lifecycle path out of `main.rs` into focused server modules. Keep the public route names stable (`/memory/explain`, `/memory/verify`, `/memory/expire`) but make the handlers delegate to smaller helpers. Extend the explain payload only where needed to expose summary-to-source traversal details already represented in the store: source path, source system, source agent, source quality, and any linked raw artifact metadata. Keep repair actions bounded and auditable: verify, expire, supersede, contest resolution, and safe metadata correction must map onto the existing lifecycle model instead of inventing a new one.</action>
  <verify>cargo test -q</verify>
  <acceptance_criteria>
    - `crates/memd-server/src/main.rs` no longer contains the core explain and repair handler bodies.
    - `crates/memd-server/src/inspection.rs` and `crates/memd-server/src/repair.rs` exist and are imported by the server.
    - `docs/api.md` still documents explain, verify, and expire as stable public routes.
  </acceptance_criteria>
  <done>Explain and repair are handled through smaller server modules without changing the public contract.</done>
</task>

<task type="auto">
  <name>Task 2: Split client rendering and command plumbing</name>
  <files>crates/memd-client/src/main.rs, crates/memd-client/src/render.rs, crates/memd-client/src/commands.rs, crates/memd-client/src/lib.rs</files>
  <read_first>crates/memd-client/src/main.rs, crates/memd-client/src/lib.rs, crates/memd-client/src/obsidian.rs</read_first>
  <action>Move the CLI summary formatting and the high-traffic command dispatch helpers out of the binary entrypoint into focused modules. Keep the command surface identical, but route working-memory, profile, source-memory, and explain output through reusable helper functions so `main.rs` stops being the place where presentation logic accumulates. Preserve existing JSON output and text summaries byte-for-byte where possible.</action>
  <verify>cargo test -q</verify>
  <acceptance_criteria>
    - `crates/memd-client/src/render.rs` exists and contains the summary render helpers.
    - `crates/memd-client/src/main.rs` is smaller and no longer owns all summary formatting.
    - `cargo test -q` still passes after the split.
  </acceptance_criteria>
  <done>Client output and command plumbing are moved into reusable modules without changing behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Harden working memory and document the v1 finish line</name>
  <files>crates/memd-server/src/working.rs, crates/memd-server/src/store.rs, docs/architecture.md, docs/source-policy.md, docs/promotion-policy.md</files>
  <read_first>crates/memd-server/src/store.rs, crates/memd-server/src/main.rs, docs/architecture.md, docs/source-policy.md, docs/promotion-policy.md</read_first>
  <action>Keep working memory deterministic but more honest: admission, eviction, and rehydration should be driven by explicit policy reasons such as freshness, trust, contradiction state, and recent use. Make the working-memory response report those reasons clearly and keep the hot-path output compact. Update the docs so the `v1` finish line is defined by the real remaining gaps, not vague memory-product language.</action>
  <verify>cargo test -q</verify>
  <acceptance_criteria>
    - `crates/memd-server/src/working.rs` exists and owns the policy helpers.
    - Working-memory responses include explicit policy reasons for eviction and rehydration.
    - The docs describe the working-memory contract in deterministic, inspectable terms.
  </acceptance_criteria>
  <done>Working memory is still deterministic, but the controller is now policy-visible and easier to reason about.</done>
</task>
</tasks>

<verification>
- [ ] `cargo test -q` succeeds
- [ ] the extracted server/client modules exist and are imported
- [ ] explain, repair, and working-memory routes still behave as documented
</verification>

<success_criteria>
- All tasks completed
- The phase closes the remaining `v1` gaps without drifting into `v2`
- The large server/client entrypoints are materially smaller and easier to reuse
- The public memory routes stay stable while the internals become modular
</success_criteria>

<output>
After completion, create `.planning/phases/01-v1-completion/01-01-SUMMARY.md`
</output>
