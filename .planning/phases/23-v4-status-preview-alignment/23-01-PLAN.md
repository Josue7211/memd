# Plan 23-01: `v4` Status Preview Alignment

## Goal

Make status diagnostics match the actual default short-term launch path.

## Steps

1. Switch bundle status preview to `current_task` intent.
2. Keep semantic fallback disabled in the preview.
3. Update README to explain what `status` is now previewing.
4. Record the phase completion in `.planning`.

## Verification

- `cargo test -q -p memd-client`
- `cargo test -q`

## Result

Bundle diagnostics stay aligned with real short-term memory behavior.
