# Plan 19-01: `v4` Hot-Path Memory Defaults

## Goal

Keep short-term memory fast by default.

## Steps

1. Add explicit semantic flags to bundle resume and handoff flows.
2. Keep semantic fallback disabled by default on the hot path.
3. Preserve semantic recall for eval and explicit deep-recall use cases.
4. Update generated bundle docs and top-level docs to match the new contract.
5. Add regression coverage for the new fast-default guidance.

## Verification

- `cargo test -q -p memd-client`
- `cargo test -q`

## Result

Short-term memory remains immediate while long-term semantic recall stays
available on demand.
