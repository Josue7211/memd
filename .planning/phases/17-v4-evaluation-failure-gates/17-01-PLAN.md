# Plan 17-01: `v4` Evaluation Failure Gates

## Goal

Make bundle evaluation usable in automation instead of only operator review.

## Steps

1. Extend `memd eval` CLI flags with explicit failure gates.
2. Add deterministic failure-reason logic for:
   - score floor failure
   - regression failure
3. Cover the gate logic with unit tests.
4. Document the new automation workflow in `README.md`.
5. Update GSD planning state to record the phase as completed.

## Verification

- `cargo test -q -p memd-client`
- `cargo test -q`
- verify `README.md` examples reflect the implemented flags

## Result

`memd eval` can serve as a bundle-memory quality gate for scripts and CI.
