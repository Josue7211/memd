# Plan 22-01: `v4` Current-Task Resume Defaults

## Goal

Make short-term memory the default launch intent.

## Steps

1. Update attach snippets to use `--intent current_task`.
2. Update generated agent shell and PowerShell profiles the same way.
3. Update bundle README and memory placeholders to match the new defaults.
4. Add regression coverage for the generated launch surfaces.
5. Record the phase completion in `.planning`.

## Verification

- `cargo test -q -p memd-client`
- `cargo test -q`

## Result

Agents start from the short-term lane by default instead of a generic resume
intent.
