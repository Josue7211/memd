# Plan 26-01: `v4` Claude Native Memory Bridge

## Goal

Bridge bundle memory into Claude Code through Claude-native import surfaces.

## Steps

1. Generate a `.memd/agents/CLAUDE_IMPORTS.md` bridge file.
2. Generate a `.memd/agents/CLAUDE.md.example` example import surface.
3. Update Claude integration docs to use imports and `/memory`.
4. Treat dream/autodream as part of the bridge contract, not a side note.
5. Verify the CLI and docs still pass.

## Verification

- `cargo test -q -p memd-client`
- `cargo test -q`

## Result

Claude Code can consume `memd` memory through Claude-native surfaces instead of
parallel markdown rituals.
