# Plan 29-01: `v4` Remember Refresh Writeback

## Goal

Keep bundle memory files aligned with durable `remember` writes.

## Steps

1. Extend `remember` so successful writes trigger a bundle resume refresh.
2. Re-render the visible bundle markdown memory files after writeback.
3. Preserve the current-task hot lane as the default refresh target.
4. Verify remember and bundle output tests.

## Verification

- `cargo test -q -p memd-client`
- `cargo test -q`

## Result

Visible short-term memory stays aligned with durable writes in the same
workflow.
