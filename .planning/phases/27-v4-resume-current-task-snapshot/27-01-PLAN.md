# Plan 27-01: `v4` Resume Current-Task Snapshot

## Goal

Make resume surfaces show actionable current-task state instead of only counts.

## Steps

1. Add a current-task snapshot block to `resume --prompt`.
2. Include the same snapshot in bundle markdown memory files.
3. Surface focus, pressure, next recovery, and active lane.
4. Keep the snapshot compact and hot-lane aligned.
5. Verify prompt and bundle tests.

## Verification

- `cargo test -q -p memd-client`
- `cargo test -q`

## Result

The hot lane reads like resumable task state, not only retrieval statistics.
