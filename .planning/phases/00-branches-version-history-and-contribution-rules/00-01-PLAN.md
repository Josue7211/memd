---
phase: 00-branches-version-history-and-contribution-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ROADMAP.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - README.md
  - CONTRIBUTING.md
  - SECURITY.md
  - docs/reference/oss-positioning.md
  - docs/policy/release-process.md
  - crates/memd-client/src/main.rs
  - crates/memd-client/src/render.rs
  - crates/memd-client/src/commands.rs
  - crates/memd-server/src/main.rs
  - crates/memd-server/src/inspection.rs
  - crates/memd-server/src/repair.rs
  - crates/memd-server/src/working.rs
autonomous: true
requirements:
  - OSS-01
  - OSS-02
  - OSS-03
  - OSS-04
  - OSS-05
user_setup: []
must_haves:
  truths:
    - "Active development is documented as branch-first and the current work is on a dedicated branch."
    - "The repository explains how to contribute, review, release, and report security issues without oral context."
    - "Large entrypoint files are split into reusable modules only at real seams, while public behavior stays stable."
  artifacts:
    - path: "docs/policy/release-process.md"
      provides: "Public branch, version history, and release conventions"
      min_lines: 60
    - path: "crates/memd-client/src/render.rs"
      provides: "CLI summary rendering helpers"
      min_lines: 120
    - path: "crates/memd-client/src/commands.rs"
      provides: "CLI command routing helpers"
      min_lines: 80
    - path: "crates/memd-server/src/inspection.rs"
      provides: "Server explain/provenance helpers"
      min_lines: 120
    - path: "crates/memd-server/src/repair.rs"
      provides: "Server repair lifecycle helpers"
      min_lines: 120
    - path: "crates/memd-server/src/working.rs"
      provides: "Server working-memory policy helpers"
      min_lines: 120
  key_links:
    - from: "README.md"
      to: "docs/policy/release-process.md"
      via: "public roadmap links to branch/release conventions"
      pattern: "release-process"
    - from: "CONTRIBUTING.md"
      to: "docs/policy/release-process.md"
      via: "contribution guidance points at the release and branch workflow"
      pattern: "branch|release"
    - from: "crates/memd-client/src/main.rs"
      to: "crates/memd-client/src/render.rs"
      via: "CLI summaries are delegated to extracted render helpers"
      pattern: "render_working_summary|render_profile_summary|render_source_summary"
    - from: "crates/memd-server/src/main.rs"
      to: "crates/memd-server/src/inspection.rs"
      via: "server explain/provenance handlers delegate to a focused module"
      pattern: "inspection::"
---

<objective>
Make the repository OSS-ready first: branch-first development, explicit version/release history, and contribution/security rules that someone outside the project can follow. While doing that, split the largest entrypoints into reusable modules only where the code already has clear seams.

Purpose: Put the repo on a real public-working workflow before the memory roadmap continues.
Output: Branch/release docs, clearer contribution rules, and smaller entrypoints with stable behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-branches-version-history-and-contribution-rules/00-CONTEXT.md

@README.md
@CONTRIBUTING.md
@SECURITY.md
@docs/reference/oss-positioning.md
@crates/memd-client/src/main.rs
@crates/memd-server/src/main.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Codify OSS branch and release workflow</name>
  <files>ROADMAP.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md, README.md, CONTRIBUTING.md, SECURITY.md, docs/reference/oss-positioning.md, docs/policy/release-process.md</files>
  <read_first>ROADMAP.md, .planning/ROADMAP.md, README.md, CONTRIBUTING.md, SECURITY.md, docs/reference/oss-positioning.md</read_first>
  <action>Update the public roadmap and contributor-facing docs so they explicitly state the branch-first workflow, version-history conventions, and release/review/security expectations. Add a focused public doc for the branch/release process if the guidance needs a stable home. Keep the language practical and specific: what branch to work on, how version history is recorded, what a contributor should do before opening a PR, and where to report security issues.</action>
  <verify>grep -n "branch" README.md CONTRIBUTING.md docs/policy/release-process.md ROADMAP.md .planning/ROADMAP.md</verify>
  <acceptance_criteria>
    - `ROADMAP.md` contains a `v0` OSS foundations section.
    - `CONTRIBUTING.md` and `SECURITY.md` still read as public-facing policies, not planning notes.
    - `docs/policy/release-process.md` exists or the equivalent guidance is present in the public docs.
  </acceptance_criteria>
  <done>Branching, version history, and contribution rules are explicit and public.</done>
</task>

<task type="auto">
  <name>Task 2: Split CLI presentation and command helpers</name>
  <files>crates/memd-client/src/main.rs, crates/memd-client/src/render.rs, crates/memd-client/src/commands.rs</files>
  <read_first>crates/memd-client/src/main.rs, crates/memd-client/src/lib.rs, crates/memd-client/src/obsidian.rs</read_first>
  <action>Extract the existing summary formatting and high-traffic dispatch helpers from the CLI binary into focused modules. Keep the CLI output stable and do not change the public command surface. The goal is to make the file easier to reuse and review, not to redesign the CLI.</action>
  <verify>cargo test -q -p memd-client</verify>
  <acceptance_criteria>
    - `crates/memd-client/src/render.rs` contains the summary render helpers.
    - `crates/memd-client/src/commands.rs` contains the extracted command routing helpers.
    - The client crate still tests cleanly after the split.
  </acceptance_criteria>
  <done>The CLI entrypoint is smaller and the presentation logic is reusable.</done>
</task>

<task type="auto">
  <name>Task 3: Split server helper clusters at existing seams</name>
  <files>crates/memd-server/src/main.rs, crates/memd-server/src/inspection.rs, crates/memd-server/src/repair.rs, crates/memd-server/src/working.rs</files>
  <read_first>crates/memd-server/src/main.rs, crates/memd-server/src/store.rs, crates/memd-server/src/routing.rs, crates/memd-server/src/keys.rs</read_first>
  <action>Extract the current explain/provenance helpers, repair lifecycle helpers, and working-memory policy helpers from the server entrypoint into focused modules. Preserve the public API routes and response shapes. The point is to split the large file where it already has real seams, so future OSS contributors can navigate the code without digging through a single giant entrypoint.</action>
  <verify>cargo test -q -p memd-server</verify>
  <acceptance_criteria>
    - `crates/memd-server/src/inspection.rs`, `repair.rs`, and `working.rs` exist and are used by `main.rs`.
    - The public memory routes still compile and behave the same.
    - The server crate still tests cleanly after the split.
  </acceptance_criteria>
  <done>The server entrypoint is slimmer and the helper seams are reusable.</done>
</task>
</tasks>

<verification>
- [ ] `cargo test -q -p memd-client` succeeds
- [ ] `cargo test -q -p memd-server` succeeds
- [ ] OSS docs clearly describe branches, releases, contribution, and security
- [ ] server/client entrypoints are materially smaller and still stable
</verification>

<success_criteria>
- All tasks completed
- The repo is branch-first and public-facing workflow is documented
- The large files are split only where the seams justify it
- The OSS foundation is in place before the deeper memory roadmap continues
</success_criteria>

<output>
After completion, create `.planning/phases/00-branches-version-history-and-contribution-rules/00-01-SUMMARY.md`
</output>
