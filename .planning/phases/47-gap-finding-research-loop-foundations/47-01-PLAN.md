---
phase: 47-gap-finding-research-loop-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/memd-client/src/main.rs
  - crates/memd-client/src/render.rs
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
portability: portable
requirements:
  - v6-gap-loop-foundations
must_haves:
  truths:
    - "Gap reports inspect repo docs, planning artifacts, eval output, git activity, and runtime wiring."
    - "Gap output stays bounded and structured instead of turning into a freeform essay."
    - "The research loop prioritizes memory quality, epistemic retrieval, and coworking safety."
  artifacts:
    - path: "crates/memd-client/src/main.rs"
      provides: "Gap-report evidence collection and prioritization"
      min_lines: 7000
    - path: "crates/memd-client/src/render.rs"
      provides: "Gap summary rendering"
      min_lines: 900
  key_links:
    - from: "ROADMAP.md"
      to: "crates/memd-client/src/main.rs"
      via: "phase 47 goal is implemented as gap evidence collection and prioritization"
      pattern: "gap|research"
    - from: ".planning/STATE.md"
      to: "crates/memd-client/src/main.rs"
      via: "open loops and current focus should be reflected in evidence"
      pattern: "gap|runtime|memory"
---

<objective>
Make the gap-finding loop real by teaching the existing `memd gap` / `GapReport`
path to inspect the repo, planning artifacts, eval outputs, git activity, and
runtime wiring as a single bounded research pass. The first slice should rank
the highest-value gaps for memory quality, epistemic retrieval, and coworking
safety without auto-editing code yet.

Purpose: give `memd` a reproducible research loop that can spot its own highest-
value gaps from live evidence.
Output: richer gap reports and summary artifacts that make the next improvement
target obvious.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-gap-finding-research-loop-foundations/47-CONTEXT.md

@ROADMAP.md
@README.md
@docs/setup.md
@crates/memd-client/src/main.rs
@crates/memd-client/src/render.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Broaden gap evidence collection</name>
  <files>crates/memd-client/src/main.rs</files>
  <read_first>crates/memd-client/src/main.rs, ROADMAP.md, .planning/STATE.md, README.md</read_first>
  <action>Expand the existing gap-report collector so it samples repo docs, planning files, git branch/status, and runtime wiring in addition to the current eval/resume/coordination/recent-commit inputs. Keep the evidence bounded, terse, and inspectable.</action>
  <verify>cargo test -q -p memd-client</verify>
  <acceptance_criteria>
    - Gap reports include repo-level evidence, not just `.planning` and bundle state.
    - Runtime wiring is surfaced as part of the research evidence.
    - The report remains bounded and machine-readable.
  </acceptance_criteria>
  <done>The gap loop sees the repo as a live system, not just a planning folder.</done>
</task>

<task type="auto">
  <name>Task 2: Keep the gap summary compact and actionable</name>
  <files>crates/memd-client/src/render.rs</files>
  <read_first>crates/memd-client/src/render.rs, crates/memd-client/src/main.rs</read_first>
  <action>Adjust the gap summary rendering only if needed to keep the research output concise, evidence-backed, and easy to scan. Do not add a second report format or a freeform narrative path.</action>
  <verify>cargo test -q -p memd-client</verify>
  <acceptance_criteria>
    - The summary remains short enough to use during active work.
    - The highest-priority gaps are easy to identify at a glance.
  </acceptance_criteria>
  <done>The research output is still usable mid-session.</done>
</task>
</tasks>

<verification>
- [ ] `cargo test -q -p memd-client` succeeds
- [ ] Gap reports include repo, planning, eval, and runtime signals
- [ ] Summary output remains bounded and actionable
</verification>

<success_criteria>
- `memd gap` can identify the highest-value gaps from live evidence instead of only manual triage
- the first slice stays bounded and does not auto-edit code
- the research loop is usable as a real operator tool, not just a concept
</success_criteria>
