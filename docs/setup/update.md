# Update memd

Secondary/reference doc. Start from [[ROADMAP]] for project truth.

```bash
git pull --ff-only
scripts/install-memd.sh
memd doctor --summary
```

Do not delete `.memd` to update. `.memd` is user/project memory state.
