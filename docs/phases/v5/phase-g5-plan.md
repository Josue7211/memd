---
phase: G5
name: AdversarialNoise Bench + V5 Completion Gate
version: v5
kind: implementation-plan
status: ready-to-execute
opened: 2026-04-22
depends_on: [A5, B5, C5, D5, E5, F5]
phase_doc: docs/phases/v5/phase-g5-adversarial-noise.md
granularity: "one step = ≤1 agent session; TDD; commit per task"
axis: raw_retrieval, trust_provenance, V5 completion gate
---

# Phase G5 — Implementation Plan

## 0. Executive summary

Two jobs in one phase: (1) adversarial-noise suite, (2) V5 aggregation gate — `memd bench substrate --all`, publishes `SUBSTRATE_BENCHMARKS.md`, reproducibility script + competitor card + 10-STAR regeneration to composite ≥5.5.

## 1. Surface area

### Files to create

| Path | Responsibility |
| --- | --- |
| `crates/memd-client/src/benchmark/substrate/adversarial_noise.rs` | G5 noise runner. |
| `crates/memd-client/src/benchmark/substrate/aggregator.rs` | Suite aggregation + markdown regenerator. |
| `.memd/benchmarks/substrate/adversarial-noise.yaml` | Spec. |
| `.memd/benchmarks/substrate/fixtures/g5/` | 50 canonical + 150 noise records. |
| `scripts/substrate-bench-reproduce.sh` | Third-party reproducibility. |
| `docs/verification/SUBSTRATE_COMPETITOR.md` | Competitor scorecard template. |
| `crates/memd-client/src/main_tests/substrate_g5_tests/mod.rs` | Tests. |

### Files to modify

| Path | Change |
| --- | --- |
| `substrate/mod.rs` | Register G5; add `--all` routing to aggregator. |
| `docs/verification/SUBSTRATE_BENCHMARKS.md` | Regenerated by aggregator per run. |
| `docs/verification/MEMD-10-STAR.md` | G5 writes V5 axis deltas. |
| Phase doc. |

## 2. Schema changes

None.

```yaml
suite: adversarial-noise
canonical_count: 50
noise_per_canonical: 3
noise_recency_offset_s: 3600   # noise appears newer
pass_gate:
  canonical_wins_rate: 0.90
  noise_leak_rate: 0.05
  tie_break_by_provenance_rate: 0.75
```

## 3. API shape

```
memd bench substrate --all \
  [--seed-base 40] \
  [--output .memd/benchmarks/substrate/results/] \
  [--regenerate-report] \
  [--regenerate-10star] \
  [--fail-fast]

memd bench substrate --suite adversarial-noise […]
bash scripts/substrate-bench-reproduce.sh
```

## 4. Test matrix

### G5 noise suite

1. `noise_generator_seeds_contradictions_with_recency_offset`
2. `scorer_canonical_wins_rate`
3. `scorer_tie_break_by_provenance_when_canonical_newest`
4. `runner_50_canonical_150_noise_complete`
5. `cli_g5_noise_happy`
6. `cli_g5_noise_reproducibility`

### Aggregator / V5 gate

7. `aggregator_runs_all_7_suites_sequential_or_parallel`
8. `aggregator_fail_fast_stops_on_first_fail`
9. `aggregator_writes_substrate_benchmarks_md` — regenerator rewrites file in place.
10. `aggregator_writes_10star_composite_section` — refuses to write if computed composite < 5.5 (unless `--allow-below-target`).
11. `reproducibility_script_matches_within_0_03_on_fresh_clone`
12. `competitor_card_template_fills` — render test with fixture competitor numbers.
13. `cli_bench_substrate_all_end_to_end_on_clean_tree`

## 5. Fixtures

`.memd/benchmarks/substrate/fixtures/g5/`:

| File | Contents |
| --- | --- |
| `canonical-50.jsonl` | 50 facts with full provenance. |
| `noise-150.jsonl` | 150 contradicting semantic records. |
| `queries.jsonl` | 50 queries, one per canonical fact. |
| `competitor-sample-mempalace.json` | Fixture competitor card input (documentation example, not real data). |

## 6. Telemetry

Aggregated results at `docs/verification/v5-runs/YYYY-MM-DD.ndjson` + regenerated `SUBSTRATE_BENCHMARKS.md`.

## 7. Feature flags

| Var | Default | Effect |
| --- | --- | --- |
| `MEMD_SUBSTRATE_AGG_PARALLEL` | `1` | Run suites in parallel where safe. |
| `MEMD_SUBSTRATE_ALLOW_BELOW_TARGET` | `0` | Gate writer refuses composite <5.5 unless set. |

## 8. Task list

### Task G5.1 — noise generator + suite

- [ ] Tests 1–4 failing.
- [ ] Commit: `feat(bench/g5): adversarial-noise suite (G5)`.

### Task G5.2 — noise CLI + pass-gate

- [ ] Tests 5 + 6 failing.
- [ ] Commit: `feat(bench/g5): noise CLI + pass-gate (G5)`.

### Task G5.3 — aggregator

- [ ] Tests 7 + 8 + 9 failing.
- [ ] Implement suite-of-suites runner + markdown regenerator.
- [ ] Green.
- [ ] Commit: `feat(bench/substrate): aggregator + SUBSTRATE_BENCHMARKS regenerator (G5)`.

### Task G5.4 — 10-STAR gate writer

- [ ] Test 10 failing.
- [ ] Implement composite calculator + gate refusal on <5.5.
- [ ] Green.
- [ ] Commit: `feat(bench/substrate): 10-STAR gate writer (G5)`.

### Task G5.5 — reproducibility script + competitor card

- [ ] Tests 11 + 12 failing.
- [ ] Write `scripts/substrate-bench-reproduce.sh` + `SUBSTRATE_COMPETITOR.md` template.
- [ ] Green.
- [ ] Commit: `feat(bench/substrate): reproducibility + competitor card (G5)`.

### Task G5.6 — end-to-end CI

- [ ] Test 13 failing.
- [ ] CI wire: nightly `memd bench substrate --all`; failure blocks main.
- [ ] Commit: `ci(bench/substrate): --all nightly + gate (G5)`.

### Task G5.7 — 7-day stability + milestone close

- [ ] 10/10 CI runs over 7 days.
- [ ] If composite ≥ 5.5: fill `docs/verification/milestones/MILESTONE-v5.md`, close V5 in ROADMAP.md, open V6.
- [ ] Commit: `docs(milestone): V5 closed, composite ≥ 5.5 (G5)`.

## 9. Bench impact

G5 is the V5 gate. No external bench impact beyond V5 closure.

## 10. Dependency graph

- Requires: A5–F5 all closed.
- Blocks: V6 entry gate.
- Strictly sequential — gate phase.

## Exit criteria (V5 milestone)

1. Noise suite tests 1–6 green.
2. Aggregator tests 7–13 green.
3. `memd bench substrate --all` passes 10/10 CI.
4. Reproducibility script matches ±0.03 from fresh clone.
5. Composite ≥ 5.5 in regenerated `MEMD-10-STAR.md`.
6. `MILESTONE-v5.md` filled.
7. ROADMAP V5 → closed.
8. Atomic commits on `research/mining`.
