---
opened: 2026-05-05
phase: v12-close
status: handoff-ready
prev_handoff: 2026-05-05-v11-closed-v12-next.md
branch: main
repo_state: clean on main after V12 close commit; ahead origin/main locally
next_step_a: start V13 Evidence + 0.1.0 Release from docs/phases/v13/V13-INTEGRATION.md
next_step_b: push local stack if remote sync is desired
public_gate_note: external stranger-review artifacts remain pending if public-review gate is required
release_note: V12 closed composite 7.75; 0.1.0 remains V13-owned per docs/verification/0.1.0-CONTRACT.md
---

# V12 Closed - V13 Next

One sentence: V12 interop SOTA closed at composite `7.75/10`; next work starts
V13 release evidence and 0.1.0 gate close.

## Pickup

```bash
cd /Volumes/T7/projects/memd
git switch main
git status --short --branch
sed -n '1,180p' docs/handoff/LATEST.md
```

Expected pickup state after this packet commits: clean `main`, ahead
`origin/main`. Push if remote sync is desired.

## Closed Gates

- A12: routine library browse/edit/merge/deprecate landed in
  `memd_core::routine::library` plus `memd routines`.
- B12: routine composition landed as explicit routine A+B=C support.
- C12: project inheritance and override cascade landed in the routine library.
- D12: cross-workspace routine export/import with checksum tamper rejection landed.
- E12/F12: universal protocol envelope covers MCP, ACP, typed-channel, and Codex
  custom surfaces with parity proof.
- G12: proof harness closed PR=8, CH=8, TP=8 and preserved SC/CR/RR/TE.
- H12/J12: signed audit entries and external tamper verification landed.
- I12: audit browse/explain/verify CLI surface landed.

## Verification

- `scripts/verify/v12-interop-sota-suite.sh` -> passed.
- `cargo fmt --check` -> passed.
- `git diff --check` -> passed.
- `cargo test -p memd-client --no-run` -> passed earlier in close session;
  client binary link on the T7 volume was stopped after compile diagnostics due
  to long link time.

## Proof Artifacts

- G12 gate:
  `docs/verification/v12-proof-runs/2026-05-05-interop-sota-suite.ndjson`
- G12 summary:
  `docs/verification/v12-proof-runs/2026-05-05-interop-sota-suite.md`
- Negative controls:
  `docs/verification/v12-proof-runs/2026-05-05-negative-controls.ndjson`
- Axis evidence:
  `docs/verification/v12-proof-runs/2026-05-05-axis-evidence/`

## V12 Proof Metrics

```json
{"scenario_count":7,"pass_count":7,"fail_count":0}
{"negative_controls_fired":4}
{"procedural_reuse":8,"cross_harness":8,"trust_provenance":8}
{"session_continuity":8,"correction_retention":7,"raw_retrieval":8,"token_efficiency":7}
{"composite":7.75,"parity_delta":0.0,"signed_audit_entries":4,"tamper_detected":true}
```

## Caveats

- V12 proof is deterministic and repo-owned; it does not fabricate external
  stranger-review artifacts.
- MCP/ACP work is the protocol-neutral envelope and parity harness, not a
  network daemon release.
- Feature-flag graduation windows remain ops spillover into V13 planning.
- Local `main` has not been pushed after the V12 stack in this session.

## Next

V13 owns 0.1.0 release evidence:

- read `docs/phases/v13/V13-INTEGRATION.md`
- confirm V13-owned release gates and scorecard delta
- collect external stranger-review artifacts if public review is required
- keep commits atomic and proof rerunnable
