---
opened: 2026-05-05
phase: v11-close
status: handoff-ready
prev_handoff: 2026-05-05-v10-closed-v11-next.md
branch: main
repo_state: dirty until this handoff commit lands; main remains ahead origin/main locally
next_step_a: start V12 interop SOTA from docs/phases/v12/V12-INTEGRATION.md
next_step_b: push local stack if remote sync is desired
public_gate_note: external stranger-review artifacts remain pending if public-review gate is required
release_note: V11 is SOTA compiler gate; 0.1.0 remains V13-owned per docs/verification/0.1.0-CONTRACT.md
---

# V11 Closed - V12 Next

One sentence: V11 compiler SOTA closed at composite `6.95/10`; next work
starts V12 interop SOTA.

## Pickup

```bash
cd /Volumes/T7/projects/memd
git switch main
git status --short --branch
sed -n '1,180p' docs/handoff/LATEST.md
```

Expected pickup state after this packet commits: clean `main`, ahead
`origin/main`. Push if remote sync is desired.

## Closed Gates

- A11: project-aware wake landed at `memd_core::isolation`.
- B11: compaction-aware recall landed at `memd_core::compaction::recovery`.
- C11: silent-correction detection landed at `memd_core::correction::silent`.
- D11: dynamic per-turn compiler landed at
  `memd_core::runtime::resume::compiler_v2`.
- E11: cost target + ledger landed at `memd_core::cost::ledger`.
- F11: wake median proof closed at `1480 <= 1500` tokens.
- G11: proof harness closed V11 with SC=8, CR=7, TE=7.
- Schema locks landed: generated `memory_items.project_id`,
  `memory_items.workspace_id`, plus `compiler_context`, `cost_ledger`, and
  `correction_flags`.

## Verification

- `scripts/verify/v11-compiler-sota-suite.sh` -> passed.
- `cargo test -p memd-core v11 -- --nocapture` -> passed.
- `cargo test -p memd-server v11_schema -- --nocapture` -> passed.

## Proof Artifacts

- G11 gate:
  `docs/verification/v11-proof-runs/2026-05-05-compiler-sota-suite.ndjson`
- G11 summary:
  `docs/verification/v11-proof-runs/2026-05-05-compiler-sota-suite.md`
- Negative controls:
  `docs/verification/v11-proof-runs/2026-05-05-negative-controls.ndjson`
- Axis evidence:
  `docs/verification/v11-proof-runs/2026-05-05-axis-evidence/`

## V11 Proof Metrics

```json
{"scenario_count":7,"pass_count":7,"fail_count":0}
{"negative_controls_fired":4}
{"session_continuity":8,"correction_retention":7,"token_efficiency":7}
{"procedural_reuse":6,"cross_harness":6,"raw_retrieval":8,"trust_provenance":6}
{"composite":6.95,"wake_median_tokens":1480,"silent_correction_latency_ms":900}
```

## Caveats

- V11 proof is repo-owned and deterministic; it does not fabricate external
  stranger-review artifacts.
- Public benchmark sweep is not re-run in this close packet. V11 did not own
  raw retrieval lift; RR stays at V10 score `8/10`.
- Feature-flag graduation windows remain a V12 planning/ops concern: project
  awareness, dynamic compiler, silent-correction detection, each with a 7-day
  clean window.
- Local `main` has not been pushed after the V11 stack in this session.

## Next

V12 owns interop SOTA:

- routine library UI and composition
- per-project routine inheritance
- cross-workspace routine export/import
- MCP/ACP/custom protocol parity
- signed audit entries and tamper-evident export

Start by reading `docs/verification/milestones/MILESTONE-v12.md` and
`docs/phases/v12/V12-INTEGRATION.md`. Keep commits atomic and keep proof
rerunnable.
