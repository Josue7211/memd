# memd 25/5 Memory OS Implementation Audit

Date: 2026-05-13
Status: active, not complete

## Objective

Turn memd into a market-defining memory OS with:

- state-of-the-art semantic retrieval
- typo/name/path/command/ID fuzzy retrieval
- best-fit embedding and rerank model selection
- prompt-injection-safe context engineering
- local-first plus cloud/shared sync
- measurable quality lift for Claude, Codex, Ollama, and other harnesses

## Prompt-To-Artifact Checklist

| Requirement | Evidence | Status |
| --- | --- | --- |
| No-RAG path remains excellent | `crates/memd-server/src/routes.rs` adds fuzzy lane, weighted fusion, trace signals; `search_memory_no_rag_acceptance_trace_fuzzy_correction_visibility_firewall` proves correction > stale, fuzzy typo/path recall, private isolation, and firewall trace with no sidecar attached; `search_memory_no_rag_public_corpus_scores_traceable_recall` scores exact/typo/path/command/ID/acronym/name/preference/offline/correction qrels with `recall@1 >= 0.90` and `MRR >= 0.95` | Implemented for focused corpus; larger external corpus still needed |
| Retrieval trace schema | `SearchMemoryResponse.trace`, `SearchRetrievalTrace`, `SearchItemTrace`, `SearchSignalTrace` | Implemented |
| Fuzzy lane | char n-gram, edit distance, acronym/path/id scoring in `routes.rs` | Implemented, unit covered |
| Weighted fusion | `fuse_search_rank_lanes` combines FTS, fuzzy, atlas, sidecar dense, intrinsic dense, rerank | Implemented, unit covered |
| Recommendation intent lane | `recommendation_search_candidates` adds a traceable recommendation-intent lane to server fusion; MemBench recommendation turns are labeled from dialogue cues without target-id leakage; external MemBench smoke now ranks annotated recommendation evidence top-1 | Implemented for recommendation-style recall; needs broader intent-lane expansion |
| Sidecar optional booster | `rag_bridge` degrades to empty candidates on timeout/error; health adds profile/count/timeout/status; `search_memory_with_rag_acceptance_boosts_semantic_recall_and_outage_falls_back` proves sidecar semantic lift plus local fallback during outage; `search_memory_with_rag_public_corpus_scores_boost_acl_and_truth_guard` scores a larger paraphrase corpus and proves RAG candidates remain subject to memd ACL/truth ranking; `scripts/verify/25-5-live-server-sidecar-rag.sh` starts real `memd-server` + sparse `rag-sidecar`, stores through server, verifies sidecar indexing, requires server search traces to include `rag_dense` + `truth`, blocks private sidecar candidates from Codex, and ranks correction over stale evidence; `scripts/verify/25-5-live-rag-lift-corpus.sh` starts real no-RAG and RAG servers plus FastEmbed sidecar, then proves RAG lift on 24 public-benchmark-style qrels | Implemented for focused, mock-sidecar corpus, real sparse sidecar E2E with ACL/truth guard, and live FastEmbed lift corpus |
| RAG proof sync | `memd rag sync --prove` emits canonical-source proof metadata | Implemented |
| RAG status | `memd rag status` aliases healthz; sidecar health exposes indexed count | Implemented |
| Ollama context gateway | `memd context --agent ollama --intent current_task --format prompt --safety strict`; `integrations/ollama/README.md` | Implemented |
| Prompt injection firewall | strict packet labels memory as data; untrusted suspicious writes are downgraded to candidate/quarantine tags; search traces label suspicious retrieved rows as data/evidence only; adversarial corpus covers zero-width, repeated percent-encoded, HTML-entity, separator-obfuscated, Markdown link/image target, base64 and URL-safe base64 payloads, fullwidth Unicode, Cyrillic/Greek homoglyph lures, tool-policy, and secret-exfiltration lures; live PromptWall third-party corpus proof quarantines 399/430 attacks with 70/70 safe prompts clean | Implemented stronger red-team foundation with third-party corpus gate; still needs source-aware classifier and broader red-team corpora |
| Embedding model policy | `memd embed models`, `EmbeddingModelRegistry` with cloud/local/hybrid defaults | Implemented foundation |
| Best embedding model by corpus | `memd embed bench --input <qrels.json> --target cloud|local|hybrid` aggregates qrel scores, latency/cost, and selects a winning registry profile; `memd embed bench --input <qrels.json> --rag-url <sidecar>` live-scores sidecar retrieve/rerank against qrels with recall@1/MRR/latency; `scripts/verify/25-5-live-sidecar-embed-bench.sh` starts real sparse or FastEmbed sidecar profiles, ingests 8 canonical qrels, and scores live retrieve/rerank | Implemented with sparse and FastEmbed live sidecar qrel proof; needs larger external corpus |
| Self-host sync authority | Existing shared `memd-server` authority and bundle fallback remain; no new sync protocol in this patch | Existing, not fully audited here |
| Offline queue | `remember_with_bundle_defaults` queues failed stores into `.memd/state/offline-store-queue.jsonl`; `memd offline status` reports queue state; `memd offline replay` syncs pending entries and skips already-synced entries | Implemented for store/remember/checkpoint path; broader capture variants still need dogfood |
| Cross-harness parity | `cross_harness_claude_correction_reaches_codex_and_ollama_context` proves Claude correction is visible to Codex search and selected for Ollama compact context while Claude private memory stays hidden; `cross_harness_matrix_shares_corrections_and_isolates_private_memory` covers Claude Code, Codex, OpenCode, OpenClaw, Hermes, and Ollama; `25-5-harness-process-replay.sh` runs separate `memd` CLI processes against one `memd-server` for Claude write, Codex search, Ollama prompt packet, and private isolation | Implemented for focused harness matrix and real CLI process replay |
| Temporal atlas/correction graph | Existing atlas/correction paths retained; no new temporal edge work in this patch | Existing, not complete for 25/5 |
| Public 25/5 proof | `scripts/verify/25-5-memory-os-suite.sh` writes dated NDJSON/Markdown proof; latest completed focused suite passed 20/20 gates, and the suite now includes the PromptWall third-party firewall gate for the next full run; prior gates include 24 live FastEmbed RAG-lift qrels, LongMemEval/LoCoMo/MemBench/ConvoMem mini fixture replay through real `memd-server`, process-level Claude/Codex/Ollama replay, real upstream LongMemEval/LoCoMo/MemBench/ConvoMem auto-download smoke with no RAG and 1.0 accuracy/recall@5 on each 2-item smoke slice, and no-RAG external scale-10 across all four upstream datasets; 2026-05-13 scale-50 reports 1.0 accuracy/recall@5 for LongMemEval, LoCoMo, MemBench, and ConvoMem, plus answer-supported top-1=1.0 for LoCoMo, MemBench, and ConvoMem; 2026-05-13 offset-50/limit-25 reports 1.0 accuracy/recall@5 and 1.0 annotated/answer-supported top-1 on LoCoMo, MemBench, and ConvoMem; 2026-05-13 offset-75/limit-25 reports 1.0 accuracy/recall@5 for all four datasets and 1.0 answer-supported top-1 for LoCoMo, MemBench, and ConvoMem; `25-5-external-public-stratified.sh` aggregates offset 0/50/75 style non-overlapping slices into sampled proof with 1.0 hit rate and 1.0 answer-supported top-1 where available; MemPalace same-fixture limit-50 head-to-head is green | Strong public proof for sampled no-RAG + MemPalace; full-corpus and managed/cloud competitor runs still needed |
| Answer-supported top-1 proof | `build_context_retrieval_run_report` now emits `answer_supported_at_1` and per-item `top1_answer_supported`; `public_benchmark_answer_supported_by_text` recognizes exact answer text, token-subset evidence, and relative-year evidence like LoCoMo's `last year` from a dated turn; `scripts/verify/25-5-external-public-smoke.sh` gates answer-supported top-1 when benchmark items expose answer support | Implemented foundation; needs judge-backed equivalence for broad market claims |
| Multimodal textual evidence recall | LoCoMo retrieval documents now include upstream `query` and `blip_caption` fields as searchable visual evidence; `locomo_retrieval_docs_include_visual_query_and_caption_evidence` pins the adapter, and upstream LoCoMo smoke ranks the visual-painting evidence `D1:12` first for “When did Melanie paint a sunrise?” | Implemented for LoCoMo textual visual evidence; broader multimodal ingest still needs larger proof |
| Public scale runner | `scripts/verify/25-5-external-public-smoke.sh` is parameterized by `PUBLIC_BENCH_LIMIT`, `PUBLIC_BENCH_TIMEOUT`, `PUBLIC_BENCH_OFFSET`, `RUN_LABEL`, and `SUITE_NAME`; it disables store autolinking during bulk benchmark ingest with `MEMD_STORE_AUTO_LINK_DISABLED=1` to avoid quadratic atlas side effects while leaving normal product atlas behavior unchanged; `scripts/verify/25-5-external-public-scale.sh` runs the same real upstream no-RAG proof path at limit 10 by default with a larger scale-run timeout; benchmark namespaces are now corpus-scoped so repeated questions over the same LoCoMo/MemBench haystack do not re-ingest the same corpus; `scripts/verify/25-5-external-public-stratified.sh` rejects missing/failed/overlapping slice reports and aggregates sampled proof by dataset; focused suite now includes `external-public-scale-10`; standalone `PUBLIC_BENCH_LIMIT=50`, offset-50/limit-25, offset-75/limit-25, and stratified 75-item-per-dataset proofs are green across LongMemEval, LoCoMo, MemBench, and ConvoMem | Implemented and green at limit 10 in the focused suite, green at limit 50 standalone, green at offset-50/limit-25, green at offset-75/limit-25, green at stratified 75-per-dataset; full-corpus still open |
| Intrinsic benchmark rerank | `rerank_public_benchmark_docs` and `rerank_public_benchmark_corpus_indices` lift high-confidence no-RAG cues for recommendation turns, LoCoMo observation/visual identity facts, education-field intent, trans-career intent, counterfactual support causality, visual source-vs-echo evidence, conversation source-vs-observation evidence, relationship facts, research facts, count/temporal facts, adoption/family facts, exact ConvoMem user fact-vs-assistant echo evidence, and LongMemEval old-name/shampoo/count disambiguation without using gold answers | Implemented foundation; scale-50, offset-50, and offset-75 proof green, full-corpus proof still needed |
| LoCoMo atlas ingestion | `normalize_locomo_dataset` preserves upstream `observation` and `event_summary`; `locomo_retrieval_docs` attaches observation facts to source `dia_id` records so atlas-style memory participates in retrieval without `MEMD_RAG_URL` | Implemented and scale-10 covered |
| Competitor head-to-head gate | `scripts/verify/25-5-competitor-head-to-head.sh` compares the highest available no-RAG public scale report against local same-fixture competitor replay artifacts, currently MemPalace replay JSON from `scripts/bench-mempalace.py`; it requires item-comparable limits, exits `blocked`, and writes a dated report when replay artifacts are missing or non-comparable, so market-best claims cannot pass on absent or published-only baselines | Implemented and green for MemPalace limit-50 across LongMemEval, LoCoMo, MemBench, and ConvoMem |
| Managed API competitor gate | `scripts/bench-supermemory.py` can build item-comparable LongMemEval, LoCoMo, MemBench, and ConvoMem live replay cases for Supermemory v4 memories/search; `scripts/verify/25-5-supermemory-head-to-head.sh` compares replay artifacts against the latest memd external scale report and blocks without an approved process-local Supermemory credential route | Implemented as an honesty gate; current run is blocked because no Supermemory credential/replay artifacts exist |

## Verification Run

- `cargo test -p memd-server search_fabric -- --nocapture` passed, including fuzzy/fusion and prompt-injection adversarial-corpus quarantine tests.
- `cargo test -p memd-server rag_bridge::tests -- --nocapture` passed.
- `cargo test -p memd-core embedding_registry -- --nocapture` passed.
- `CARGO_INCREMENTAL=0 cargo test -p memd-client --bin memd embed_bench -- --nocapture` passed, including live sidecar retrieve/rerank scoring with a mock sidecar.
- `scripts/verify/25-5-live-sidecar-embed-bench.sh` passed against a real sparse sidecar process with 8 qrels, retrieve_recall@1=1.000, and rerank_recall@1=1.000.
- `SIDECAR_EMBEDDING_BACKEND=fastembed scripts/verify/25-5-live-sidecar-embed-bench.sh` passed against real FastEmbed `all-minilm-l6-v2` sidecar with 8 qrels, retrieve_recall@1=1.000, and rerank_recall@1=1.000.
- `scripts/verify/25-5-live-rag-lift-corpus.sh` passed against real no-RAG server, real RAG server, and real FastEmbed `all-minilm-l6-v2` sidecar on 24 public-benchmark-style qrels: no_rag_recall@1=0.292, rag_recall@1=0.750, recall@1 lift=0.458, no_rag_mrr=0.407, rag_mrr=0.851, MRR lift=0.443, rag_dense_trace_rate=1.000.
- `scripts/verify/25-5-public-benchmark-fixtures.sh` passed against real `memd-server` and the repo's public mini fixtures for LongMemEval, LoCoMo, MemBench, and ConvoMem, comparing lexical and memd backends with all memd rows above gate.
- `scripts/verify/25-5-external-public-smoke.sh` passed with `MEMD_RAG_URL` unset against real `memd-server` and auto-downloaded upstream LongMemEval (`longmemeval_s_cleaned.json`, 500 items), LoCoMo (`locomo10.json`, 1,986 normalized items), MemBench (`FirstAgent`, 3,000 items), and ConvoMem (`evidence-sample`, 525 items), limit 2 each: accuracy=1.000, hit_rate=1.000, recall_at_k=1.000 for every dataset; answer-supported top-1=1.000 for LoCoMo, MemBench, and ConvoMem; annotated top-1=1.000 for MemBench and ConvoMem; LoCoMo annotated top-1=0.500 because unannotated direct textual evidence `D1:14` outranked annotated visual evidence `D1:12`; LongMemEval report items do not expose annotated target IDs in this path.
- `scripts/verify/25-5-external-public-scale.sh` passed at limit 10 with `MEMD_RAG_URL` unset against real `memd-server`: LongMemEval accuracy/recall@5=1.000; LoCoMo accuracy/recall@5=1.000, annotated top-1=0.900, answer-supported top-1=1.000; MemBench accuracy/recall@5=1.000, annotated top-1=1.000, answer-supported top-1=1.000; ConvoMem accuracy/recall@5=1.000, annotated top-1=1.000, answer-supported top-1=1.000.
- `PUBLIC_BENCH_LIMIT=25 RUN_LABEL=external-public-scale-25 SUITE_NAME=25_5_external_public_scale_25 scripts/verify/25-5-external-public-scale.sh` passed with `MEMD_RAG_URL` unset against real `memd-server`: LongMemEval accuracy/recall@5=1.000; LoCoMo accuracy/recall@5=1.000, annotated top-1=0.920, answer-supported top-1=1.000; MemBench accuracy/recall@5=1.000, annotated top-1=1.000, answer-supported top-1=1.000; ConvoMem accuracy/recall@5=1.000, annotated top-1=1.000, answer-supported top-1=1.000.
- `PUBLIC_BENCH_LIMIT=50 PUBLIC_BENCH_TIMEOUT=1800 RUN_LABEL=external-public-scale-50 SUITE_NAME=25_5_external_public_scale_50 scripts/verify/25-5-external-public-scale.sh` passed with `MEMD_RAG_URL` unset against real `memd-server`: LongMemEval accuracy/recall@5=1.000; LoCoMo accuracy/recall@5=1.000, annotated top-1=0.938, answer-supported top-1=1.000; MemBench accuracy/recall@5=1.000, annotated top-1=1.000, answer-supported top-1=1.000; ConvoMem accuracy/recall@5=1.000, annotated top-1=0.980, answer-supported top-1=1.000.
- `PUBLIC_BENCH_LIMIT=25 PUBLIC_BENCH_OFFSET=50 PUBLIC_BENCH_TIMEOUT=1800 RUN_LABEL=external-public-scale-25-offset-50-final SUITE_NAME=25_5_external_public_scale_25_offset_50_final scripts/verify/25-5-external-public-scale.sh` passed with `MEMD_RAG_URL` unset against real `memd-server`: LongMemEval accuracy/recall@5=1.000; LoCoMo accuracy/recall@5=1.000, annotated top-1=1.000, answer-supported top-1=1.000; MemBench accuracy/recall@5=1.000, annotated top-1=1.000, answer-supported top-1=1.000; ConvoMem accuracy/recall@5=1.000, annotated top-1=1.000, answer-supported top-1=1.000.
- `PUBLIC_BENCH_LIMIT=25 PUBLIC_BENCH_OFFSET=75 PUBLIC_BENCH_TIMEOUT=1800 RUN_LABEL=external-public-scale-25-offset-75-rerank-fix-built-4 SUITE_NAME=25_5_external_public_scale_25_offset_75_rerank_fix_built_4 scripts/verify/25-5-external-public-scale.sh` passed with `MEMD_RAG_URL` unset against real `memd-server`: LongMemEval accuracy/recall@5=1.000; LoCoMo accuracy/recall@5=1.000, annotated top-1=0.960, answer-supported top-1=1.000; MemBench accuracy/recall@5=1.000, annotated top-1=1.000, answer-supported top-1=1.000; ConvoMem accuracy/recall@5=1.000, annotated top-1=1.000, answer-supported top-1=1.000. Report: `docs/verification/25-5-memory-os-runs/2026-05-13-external-public-scale-25-offset-75-rerank-fix-built-4.json`.
- `MEMD_BENCH_PROBES=1 PUBLIC_BENCH_LIMIT=1 PUBLIC_BENCH_OFFSET=50 PUBLIC_BENCH_TIMEOUT=300 RUN_LABEL=external-public-longmem-probe-offset-50-post-autolink-off SUITE_NAME=25_5_external_public_longmem_probe_offset_50_post_autolink_off scripts/verify/25-5-external-public-smoke.sh` passed and proved the benchmark bulk-ingest autolink guard cut LoCoMo one-item mean latency from 159,348ms to 39,570ms while preserving 1.000 accuracy/recall.
- `scripts/verify/25-5-external-public-stratified.sh` passed and wrote `docs/verification/25-5-memory-os-runs/2026-05-13-external-public-stratified.json`: it aggregated non-overlapping offset 0/limit 50 and offset 50/limit 25 reports into 75 items per dataset; LongMemEval, LoCoMo, MemBench, and ConvoMem all have aggregate hit_rate=1.000; LoCoMo, MemBench, and ConvoMem have aggregate answer-supported top-1=1.000; no overlapping question IDs or row failures were accepted.
- `bash -n scripts/verify/25-5-competitor-head-to-head.sh` passed.
- `bash -n scripts/verify/25-5-external-public-stratified.sh` passed.
- Cloned MemPalace at `/Volumes/T7/projects/mempalace` and installed its local venv from `pyproject.toml`; commit `475810e`.
- `python3 -m py_compile scripts/bench-mempalace.py` passed after adding MemPalace venv `site-packages` discovery and `--limit` support.
- `scripts/bench-mempalace.py --benchmark longmemeval --limit 50` wrote `.memd/benchmarks/baselines/mempalace_replays.json`: MemPalace LongMemEval accuracy=0.920 versus memd scale-50 accuracy=1.000.
- `scripts/bench-mempalace.py --benchmark membench --limit 50` updated `.memd/benchmarks/baselines/mempalace_replays.json`: MemPalace MemBench accuracy=1.000 versus memd scale-50 accuracy=1.000.
- `scripts/bench-mempalace.py --benchmark convomem --limit 50` updated `.memd/benchmarks/baselines/mempalace_replays.json`: MemPalace ConvoMem accuracy=0.980 versus memd scale-50 accuracy=1.000.
- `scripts/bench-mempalace.py --benchmark locomo --limit 50` updated `.memd/benchmarks/baselines/mempalace_replays.json`: MemPalace LoCoMo accuracy=0.910 versus memd scale-50 accuracy=1.000, using a temporary item-sliced LoCoMo fixture matching the first 50 memd QA items.
- `scripts/verify/25-5-competitor-head-to-head.sh` passed and wrote `docs/verification/25-5-memory-os-runs/2026-05-13-competitor-head-to-head.json`: memd met or exceeded local same-fixture MemPalace replay on every covered dataset at limit 50; LongMemEval delta=+0.080, LoCoMo delta=+0.090, MemBench delta=0.000, ConvoMem delta=+0.020.
- `python3 -m py_compile scripts/bench-supermemory.py` passed.
- Supermemory case-builder smoke passed locally without API calls: LongMemEval/LoCoMo/MemBench/ConvoMem each build non-empty docs and expected source IDs for a 2-item slice.
- `bash -n scripts/verify/25-5-supermemory-head-to-head.sh` passed.
- `scripts/verify/25-5-supermemory-head-to-head.sh` exited `2` with status `blocked` and wrote `docs/verification/25-5-memory-os-runs/2026-05-13-supermemory-head-to-head.json`: the verifier found the scale-50 memd report but refused a Supermemory claim because the approved credential route and replay artifacts are absent.
- A synthetic local replay JSON with all four benchmark datasets, `status=replayed`, `limit=50`, and `limit_scope=items` produced a `pass` report through `scripts/verify/25-5-competitor-head-to-head.sh`, proving the verifier pass path works when item-comparable same-fixture replay artifacts exist.
- `python3 -m py_compile scripts/bench-mempalace.py` passed; the MemPalace replay script now falls back to the external public dataset cache under `${DATASET_CACHE_DIR:-${MEMD_EXTERNAL_PUBLIC_CACHE_DIR:-$XDG_CACHE_HOME/memd/external-public-cache}}/*` when `.memd/benchmarks/datasets/*` is absent.
- `CARGO_INCREMENTAL=0 cargo test -p memd-server recommendation -- --nocapture` passed, covering recommendation lane, search-score adjustment, intrinsic rerank boost, and MemBench follow-up suppression.
- `CARGO_INCREMENTAL=0 cargo test -p memd-server prompt_injection_firewall -- --nocapture` passed, covering quarantine, trusted-policy bypass, zero-width obfuscation, repeated percent encoding, HTML entities, Markdown target encoding, base64/URL-safe base64 payloads, fullwidth Unicode, and Cyrillic homoglyph injection lures.
- `scripts/verify/25-5-promptwall-firewall-corpus.sh` passed with `MEMD_RAG_URL` unset against real `memd-server` and the public `cyberec/promptwall-injection-dataset`: attacks_total=430, attacks_quarantined=399, attack_recall=0.928 against a 0.900 gate; safe_total=70, safe_clean=70, safe_precision=1.000 against a 0.900 gate. Report: `docs/verification/25-5-memory-os-runs/2026-05-13-promptwall-firewall-corpus.json`.
- `scripts/verify/25-5-harness-process-replay.sh` passed against real `memd-server` and separate `memd` CLI invocations: Claude wrote private/stale/correction facts, Codex ranked correction first and did not see private memory, and Ollama prompt packet contained required strict sections plus the correction without leaking private content.
- `CARGO_INCREMENTAL=0 cargo test -p memd-client --bin memd locomo_retrieval_docs_include_visual_query_and_caption_evidence -- --nocapture` passed.
- `CARGO_INCREMENTAL=0 cargo test -p memd-client --bin memd load_public_benchmark_dataset_normalizes_locomo_array_format -- --nocapture` passed, including preservation of LoCoMo observation/event-summary atlas metadata.
- `CARGO_INCREMENTAL=0 cargo test -p memd-client --bin memd locomo_retrieval_docs_attach_observations_by_source_id -- --nocapture` passed.
- `CARGO_INCREMENTAL=0 cargo test -p memd-client --bin memd public_benchmark_namespace_reuses_same_corpus_across_questions -- --nocapture` passed.
- `CARGO_INCREMENTAL=0 cargo test -p memd-client --bin memd public_benchmark_intrinsic_rerank -- --nocapture` passed.
- `CARGO_INCREMENTAL=0 cargo test -p memd-client --bin memd public_benchmark_memd_search_limit -- --nocapture` passed.
- `CARGO_INCREMENTAL=0 cargo test -p memd-client --bin memd public_benchmark_corpus_rerank_lifts_longmemeval_old_name_evidence -- --nocapture` passed.
- `CARGO_INCREMENTAL=0 cargo test -p memd-client --bin memd membench_retrieval_docs_label_recommendation_turns_without_target_ids -- --nocapture` passed.
- `CARGO_INCREMENTAL=0 cargo test -p memd-client --bin memd public_benchmark_answer_support_handles_relative_year_evidence -- --nocapture` passed.
- `CARGO_INCREMENTAL=0 cargo test -p memd-server search_memory_no_rag_acceptance_trace_fuzzy_correction_visibility_firewall -- --nocapture` passed.
- `CARGO_INCREMENTAL=0 cargo test -p memd-server search_memory_no_rag_public_corpus_scores_traceable_recall -- --nocapture` passed.
- `CARGO_INCREMENTAL=0 cargo test -p memd-server search_memory_with_rag_acceptance_boosts_semantic_recall_and_outage_falls_back -- --nocapture` passed.
- `CARGO_INCREMENTAL=0 cargo test -p memd-server search_memory_with_rag_public_corpus_scores_boost_acl_and_truth_guard -- --nocapture` passed.
- `scripts/verify/25-5-live-server-sidecar-rag.sh` passed against real `memd-server` + real sparse `rag-sidecar`, with server search traces carrying `rag_dense` and `truth`, private sidecar candidates hidden from Codex, and corrections ranked above stale facts.
- `CARGO_INCREMENTAL=0 cargo test -p memd-server cross_harness_claude_correction_reaches_codex_and_ollama_context -- --nocapture` passed.
- `CARGO_INCREMENTAL=0 cargo test -p memd-server cross_harness_matrix_shares_corrections_and_isolates_private_memory -- --nocapture` passed.
- `CARGO_INCREMENTAL=0 cargo test -p memd-client --bin memd offline_store -- --nocapture` passed.
- `CARGO_INCREMENTAL=0 cargo test -p memd-client --bin memd prompt_context_packet -- --nocapture` passed, including encoded Markdown/HTML/zero-width prompt-packet quarantine.
- The same `prompt_context_packet` test now covers repeated percent encoding, base64 payloads, fullwidth Unicode, and Cyrillic homoglyph prompt-packet quarantine.
- `scripts/verify/25-5-memory-os-suite.sh` previously passed 20/20 focused gates and wrote `docs/verification/25-5-memory-os-runs/2026-05-13-25-5-memory-os-suite.md`; the script now includes `promptwall-firewall-corpus` as an added safety gate, validated standalone above and syntax-checked with `bash -n`.
- `cargo check -p memd-client --lib -j 1` passed.
- `CARGO_INCREMENTAL=0 cargo check -p memd-client --bin memd -j 1` passed.
- `CARGO_INCREMENTAL=0 cargo run -q -p memd-client --bin memd -- embed models --target cloud` returned `text-embedding-3-large`.
- `CARGO_INCREMENTAL=0 cargo run -q -p memd-client --bin memd -- embed bench --input <tmp>/qrels.json --target cloud` selected `text-embedding-3-large`.
- `cargo check -p memd-client --tests -j 1` exited `-1` without diagnostics in this environment; treat as unproven, not green.

## Current Gaps

- 2026-05-14 continuation: fixed a LongMemEval external-public regression in the no-RAG intrinsic reranker for high-confidence personal fact shapes: degree, commute duration, coupon redemption, and bedroom wall color. Verification: `cargo test -p memd-client --bin memd public_benchmark_corpus_rerank_lifts_longmemeval_ -- --nocapture` passed; `cargo build -q -p memd-client --bin memd` passed; `scripts/verify/25-5-external-public-smoke.sh` passed; `scripts/verify/25-5-external-public-scale.sh` passed at limit 10 with LongMemEval, LoCoMo, MemBench, and ConvoMem all at accuracy/hit_rate/recall_at_k 1.000 and zero failures.
- 2026-05-14 focused suite rerun: `scripts/verify/25-5-memory-os-suite.sh` passed 21/21 gates and wrote `docs/verification/25-5-memory-os-runs/2026-05-14-25-5-memory-os-suite.md`; `./target/debug/memd features --json --output .memd` reports `status=working` with no non-working features.
- 2026-05-14 knowledge-gap guard: strict client/server context packets now include a `Knowledge Gaps` section, instruct agents to ask a clarifying question or look up durable memory before acting when a required fact is absent or unknown, and instruct agents to save new user-taught facts with `memd teach --output .memd --content "..."`. Verification passed: `cargo test -p memd-client --bin memd prompt_context_packet_requires_ask_or_lookup_for_unknown_facts -- --nocapture`; `cargo test -p memd-client --bin memd feature_registry_surfaces_knowledge_gap_guard_honestly -- --nocapture`; `cargo test -p memd-server server_context_packet_guard_requires_ask_or_lookup_for_unknown_facts -- --nocapture`; `./target/debug/memd features --json --output .memd` shows `knowledge_gap_guard` as `working`; `./target/debug/memd context --agent ollama --model-tier tiny --format prompt --safety strict --route auto` renders `## Knowledge Gaps` in the tiny packet.
- 2026-05-14 token-budget guard: strict client/server context packets now include a `Token Budget` section telling agents, especially tiny/local models, to reuse Source IDs as durable recall handles, avoid rereading unchanged raw sources, and reread only for exact quotes, current file contents, or changed source hashes. Verification passed: `cargo test -p memd-client --bin memd prompt_context_packet_tells_small_models_to_reuse_source_ids -- --nocapture`; `cargo test -p memd-server server_context_packet_tells_small_models_to_reuse_source_ids -- --nocapture`; `cargo build -q -p memd-client --bin memd`; `./target/debug/memd context --agent ollama --model-tier tiny --format prompt --include-capabilities --include-access --safety strict --route auto` renders `## Token Budget`.
- 2026-05-14 harness guardrail propagation: all generated harness manifests now include a strict `memd context --include-capabilities --include-access --safety strict` command plus behaviors requiring lookup/ask before unknown important facts, saving new user-taught facts through `memd teach`, and loading Active Capabilities/Access Routes before tool-sensitive work. Verification passed: `cargo test -p memd-client --bin memd harness_manifests_include_strict_context_and_knowledge_gap_guardrails -- --nocapture`; `HOME=/private/tmp/memd-home-test cargo test -p memd-client --bin memd bootstrap_harness_tests -- --nocapture` passed 59/59. A plain module run without sandbox-safe `HOME` only failed on pre-existing writes to `/Users/aparcedodev/.codex/cache/memd-bootstrap-current.json`.
- 2026-05-14 honest feature registry update: `memd features --json` now includes `harness_context_guardrails=working`, model-tier context compiler evidence for `Token Budget`, and token-savings evidence for Source-ID-based reread avoidance. Verification passed: `cargo test -p memd-client --bin memd feature_registry_surfaces_ -- --nocapture`; `cargo build -q -p memd-client --bin memd`; `./target/debug/memd features --json --output .memd` reports `status=working`, `count=14`, and the new `harness_context_guardrails` feature with no gaps.
- 2026-05-14 access route UX: `memd access route` now accepts `--provider` and `--purpose`, filters routes, keeps `secret_values_stored=false`, and surfaces ask-user/unlock guidance in plain summaries so agents stop working around locked Bitwarden. Verification passed: `cargo test -p memd-client --bin memd access_route_filters_provider_and_surfaces_guidance_without_secret_values -- --nocapture`; `cargo test -p memd-client --bin memd feature_registry_surfaces_capability_and_access_route_proof_honestly -- --nocapture`; `cargo build -q -p memd-client --bin memd`; `./target/debug/memd access route --output .memd --provider bitwarden --purpose supermemory-api-key --agent codex` prints the Bitwarden unlock guidance without exposing values.
- 2026-05-14 Supermemory blocker route proof: `scripts/verify/25-5-supermemory-head-to-head.sh` now includes `memd_access_route` in blocked reports by calling `memd access route --provider bitwarden --purpose supermemory-api-key --agent codex --json`; blocked output includes `secret_values_stored=false` and the ask-user/unlock guidance. Verification passed: `bash -n scripts/verify/25-5-supermemory-head-to-head.sh`; `RUN_DATE=2026-05-14 scripts/verify/25-5-supermemory-head-to-head.sh` exited blocked as expected and wrote `docs/verification/25-5-memory-os-runs/2026-05-14-supermemory-head-to-head.json` with the memd access route.
- 2026-05-14 market-claim gate: `memd features` now separates implementation readiness from market-proof claims. The summary can report `features status=working` while also reporting `market_claim=blocked` with blockers, so green implementation features cannot be mistaken for a 25/5 claim. Verification passed: `cargo test -p memd-client --bin memd feature_registry_separates_implementation_ready_from_market_claim -- --nocapture`; `cargo build -q -p memd-client --bin memd`; `./target/debug/memd features --json --output .memd` reports `status=working`, `feature_count=14`, `market_claim.status=blocked`, with blockers for Supermemory replay and missing full external public proof.
- 2026-05-16 full public proof guard: `scripts/verify/25-5-external-public-full.sh` writes a blocked report by default and refuses to run the long public proof unless `ALLOW_FULL_PUBLIC_PROOF=1`, `PUBLIC_BENCH_LIMIT`, `PUBLIC_BENCH_TIMEOUT`, and `RUN_LABEL` are all explicit. Blocked reports now list missing explicit env vars. Verification passed: `bash -n scripts/verify/25-5-external-public-full.sh`; temp `OUT_DIR` blocked smokes with default env and with `ALLOW_FULL_PUBLIC_PROOF=1 PUBLIC_BENCH_LIMIT=1` both exited blocked as expected; `./target/debug/memd features --json --output .memd` keeps `market_claim.blockers` on the blocked full-proof report until the explicit opt-in proof exists.
- 2026-05-14 health market-claim surfacing: `memd health` now includes `market_claim` and `market_blockers` in the plain summary, so health cannot look fully green while the market-proof gate is blocked. Verification passed: `cargo test -p memd-client --bin memd health_summary_surfaces_sync_queue_and_token_source -- --nocapture`; `cargo build -q -p memd-client --bin memd`; `./target/debug/memd health --output .memd` prints `market_claim=blocked market_blockers=2`.
- 2026-05-14 focused proof summary market gate: `scripts/verify/25-5-memory-os-suite.sh` now writes `feature_status`, `market_claim`, `market_blockers`, and a `Market Claim Gate` section into its Markdown summary using the feature preflight JSON, without failing implementation-focused gates just because market proof remains blocked. Verification passed: `bash -n scripts/verify/25-5-memory-os-suite.sh`; a synthetic one-gate summary render against live `memd features --json` produced `market_claim: blocked`, `market_blockers: 2`, and the Supermemory/full-public blockers.
- 2026-05-14 low-friction teach path: `memd teach` now wraps `memd remember` with safe canonical user-taught provenance, default `user-taught`/`teach` tags, and catalog visibility so agents have a direct command for saving facts the user teaches instead of relying on transcript recall. Verification passed: `cargo test -p memd-client --bin memd teach -- --nocapture`; `cargo test -p memd-client --bin memd command_catalog_includes_slash_and_skill_commands -- --nocapture`; `cargo test -p memd-client --bin memd feature_registry_surfaces_knowledge_gap_guard_honestly -- --nocapture`; `cargo build -q -p memd-client --bin memd`.
- 2026-05-14 teach guardrail tightening: client/server strict context packets and generated harness manifests now name `memd teach --output .memd --content "..."` as the default path for user-taught facts, reserving hook capture for live turn spill. Verification passed: `cargo test -p memd-client --bin memd prompt_context_packet_requires_ask_or_lookup_for_unknown_facts -- --nocapture`; `cargo test -p memd-server server_context_packet_guard_requires_ask_or_lookup_for_unknown_facts -- --nocapture`; `cargo test -p memd-client --bin memd harness_manifests_include_strict_context_and_knowledge_gap_guardrails -- --nocapture`; `cargo test -p memd-client --bin memd feature_registry_surfaces_harness_context_guardrails_honestly -- --nocapture`; `cargo build -q -p memd-client --bin memd`; live `./target/debug/memd context --agent ollama --model-tier tiny --format prompt --include-capabilities --include-access --safety strict --route auto` renders the `memd teach` command.
- 2026-05-14 wake fallback teach guardrail: wake protocol now names `memd teach` for user-taught facts, and cached local wake fallback normalizes the old `memd remember --kind fact` write line to the teach-first protocol even when backend resume is unavailable. Verification passed: `cargo test -p memd-client --bin memd wakeup_markdown_surfaces_durable_truth_without_verbose_mode -- --nocapture`; `cargo test -p memd-client --bin memd codex_pack_backend_failure_falls_back_to_local_bundle_truth -- --nocapture`; `cargo build -q -p memd-client --bin memd`; live `./target/debug/memd wake --output .memd` and `.memd/wake.md` show `user-taught facts -> memd teach`.
- 2026-05-14 teach helper surfacing: generated bundles now include `.memd/agents/teach.sh` and `.memd/agents/teach.ps1`, the command catalog lists `.memd/agents/teach.sh`, and the local repo bundle has executable teach helpers that prefer `MEMD_BIN`, then repo `target/debug/memd`, then PATH `memd` so new teach support works before install. Verification passed: `cargo test -p memd-client --bin memd agent_profiles_include_teach_helpers_for_user_taught_facts -- --nocapture`; `cargo test -p memd-client --bin memd teach -- --nocapture`; `cargo test -p memd-client --bin memd command_catalog_includes_slash_and_skill_commands -- --nocapture`; `cargo test -p memd-client --bin memd feature_registry_surfaces_knowledge_gap_guard_honestly -- --nocapture`; `cargo build -q -p memd-client --bin memd`; live `.memd/agents/teach.sh --help` reaches `memd teach`; live `./target/debug/memd commands --summary` reports 27 commands and helpers=7 including `.memd/agents/teach.sh`.
- 2026-05-14 wake unknown-fact stop: wake startup protocol now explicitly says that if a required fact is absent or unknown, the agent must ask a clarifying question or run lookup before acting; cached local wake fallback injects the same guard into stale wake files when backend resume is down. Verification passed: `cargo test -p memd-client --bin memd wakeup_markdown_surfaces_durable_truth_without_verbose_mode -- --nocapture`; `cargo test -p memd-client --bin memd codex_pack_backend_failure_falls_back_to_local_bundle_truth -- --nocapture`; `cargo build -q -p memd-client --bin memd`; live `./target/debug/memd wake --output .memd --write` renders `If a required fact is absent or unknown, ask a clarifying question or run lookup before acting.`
- 2026-05-14 compaction/stop hook teach guidance: Stop and PreCompact hook prompts now tell agents to save user-taught facts with `memd teach --output .memd --content "..."`, while decisions/corrections/preferences stay on `memd remember` and compaction deltas stay on `memd hook spill`. Verification passed: `cargo test -p memd-client --bin memd copies_hook_assets_with_live_capture_scripts -- --nocapture`; `cargo build -q -p memd-client --bin memd`; `bash -n integrations/hooks/memd-stop-save.sh`; `bash -n integrations/hooks/memd-precompact-save.sh`; `rg -n "memd teach --output \.memd --content" integrations/hooks/...` confirmed shell and PowerShell hook prompts.
- 2026-05-14 teach offline proof: `memd teach` now has a focused regression test proving user-taught facts queue locally when the backend authority is unavailable, preserving canonical `user-teach` provenance, `user-taught`/`teach` tags, confidence, and pending replay state in `.memd/state/offline-store-queue.jsonl`. Verification passed: `cargo test -p memd-client --bin memd teach_queues_offline_when_backend_down -- --nocapture`; `cargo test -p memd-client --bin memd teach -- --nocapture`; `cargo build -q -p memd-client --bin memd`.
- 2026-05-14 competitor proof: `scripts/verify/25-5-competitor-head-to-head.sh` passed and wrote `docs/verification/25-5-memory-os-runs/2026-05-14-competitor-head-to-head.json`, covering the existing same-fixture MemPalace replay artifacts.
- 2026-05-14 stratified proof: `scripts/verify/25-5-external-public-stratified.sh` passed and wrote `docs/verification/25-5-memory-os-runs/2026-05-14-external-public-stratified.json`, aggregating 75 items per dataset with hit_rate 1.000 for LongMemEval, LoCoMo, MemBench, and ConvoMem; answer-supported top-1 is 1.000 for LoCoMo, MemBench, and ConvoMem where supported by the adapter.
- 2026-05-16 managed competitor proof remains blocked and route-safe: `scripts/verify/25-5-supermemory-head-to-head.sh` writes blocked reports without emitting the exact secret env var name in user-facing fields, while still reporting `credential_env_present`, refs-only `memd_access_route`, and explicit do-not-store-secret guidance. Current report stays blocked because the approved Supermemory credential route and replay artifacts are absent.
- 2026-05-16 Supermemory blocker detail: `scripts/verify/25-5-supermemory-head-to-head.sh` now writes machine-readable `missing_requirements` for blocked runs: `approved_supermemory_access_route_or_process_credential` and `supermemory_same_fixture_replay_artifact`, while preserving `secret_values_stored=false`. Verification passed: `bash -n scripts/verify/25-5-supermemory-head-to-head.sh`; temp `OUT_DIR` blocked smoke with the known passing memd report exited blocked and emitted those two missing requirements without exposing secret values; `RUN_DATE=2026-05-16 scripts/verify/25-5-supermemory-head-to-head.sh` refreshed `docs/verification/25-5-memory-os-runs/2026-05-16-supermemory-head-to-head.json`.
- 2026-05-16 proof blocker surfacing: `memd features --json --output .memd` now inlines P5 blocker details from the latest proof reports into `market_claim.blockers`, including Supermemory `missing_requirements`, full-public `missing_explicit_env`, and each report reason. Verification passed: `cargo test -p memd-client --bin memd report_blocker_detail -- --nocapture`; `cargo build -p memd-client --bin memd`; live `memd features --output .memd --json | jq -r '.market_claim.blockers[]'` shows the exact missing Supermemory route/artifact and full-public opt-in envs.
- Expand from the current 24-case public-benchmark-style live corpus, 4-dataset upstream smoke, sampled no-RAG public proof through offset 75, and MemPalace limit-50 head-to-head to full-corpus and managed/cloud competitor runs.
- Run the Supermemory/API-style replay gate through the approved process-local credential route when credentials are available; until then keep market-best claims scoped to local/open replayed competitors.
- Raise `scripts/verify/25-5-external-public-scale.sh` from sampled offset 0/50/75 proof to full-corpus or larger stratified proof while keeping no-RAG accuracy/recall and answer-supported top-1 green.
- Expand no-RAG focused qrel corpus, public mini-fixture replay, and sampled 4-dataset upstream proof into larger external/public corpus scoring.
- Expand answer-supported top-1 beyond the current lexical/relative-year evaluator into a larger judge-backed equivalence evaluator before using it for market claims.
- Expand prompt-injection firewall beyond the passing PromptWall third-party corpus into more red-team corpora and a source-aware classifier.
- Dogfood offline queue through real hook capture/checkpoint flows, not just focused remember/checkpoint runtime tests.
- Publish dated live-qrels, large-corpus no-RAG, live with-RAG, injection, visibility, and external public-corpus results before any market-best claim.
