# Changelog

All notable changes to `memd` will be documented in this file.

The project currently uses an `Unreleased` section while the OSS workflow is
being finalized.

## Unreleased

- branch-first workflow and release process documentation
- milestone plus scoped feature branch model for phased work
- roadmap and planning structure for phased work
- ongoing memory substrate and control plane improvements
- resume pressure and stale-session guidance for compact short-term recovery
- redundant context detection and compacted resume rendering to reduce prompt bloat
- Obsidian compiled-artifact reuse so repeated compile calls do not reopen the same source unnecessarily
- Obsidian scan fingerprint caching with reuse for unchanged, renamed, and touched-but-identical vault files
- Obsidian status readiness reporting, including cache health and missing setup files
- bundle quickstart/readiness docs for easier local setup
